import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles(() => ({
  paper: {
    paddingTop: '2em',
    paddingLeft: '2em',
    paddingRight: '2em',
    paddingBottom: '2em',
    marginTop: '2em',
    marginLeft: '2em',
    maxWidth: '40em'
  }
}));
